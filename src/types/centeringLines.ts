export interface CenteringLines {
  top: { outer: number; inner: number };
  bottom: { outer: number; inner: number };
  left: { outer: number; inner: number };
  right: { outer: number; inner: number };
}
