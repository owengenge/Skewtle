// Output canvas
export const OUTPUT_W = 400;
export const PAD = 30; // inset from canvas edge to avoid clipping rounded card corners

// Input stage
export const MAX_WIDTH = Math.min(800, window.innerWidth - 32);
export const MAX_HEIGHT = Math.max(MAX_WIDTH * (3 / 4), window.innerHeight * 0.65);
